# Image-Colorization
We implemented and compared multiple image colorization approaches, starting from basic CNNs and U-Net models to a pretrained ResNet-based architecture. We trained and fine-tuned the models on a landscape dataset, evaluated them using both pixel-based and perceptual metrics.
